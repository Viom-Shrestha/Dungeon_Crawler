# Dungeon Crawler Game Requirements
# Core game (text-based) - no external packages required
# GUI version requires Pygame

# For GUI version:
pygame>=2.5.0

# The game uses these built-in modules:
# - random: for generating random maps and encounters
# - time: for game timing and delays
# - typing: for type hints (Python 3.5+)
# - enum: for defining game constants
